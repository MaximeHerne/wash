<div class="container text-center">
<p>
  Hello <%= @order.user.profile.first_name %>,<br>
  Ta commande est confirmée, voici ce que tu as commandé : <br>
  Une cycle de machine de <%= @order.temperature %>. <br>
  Ton adresse : <%= @order.user.profile.address %>, <%= @order.user.profile.postal_code %> <%= @order.user.profile.locality %><br>
  Nous passons récupérer votre linge le <strong><%= @order.pickup_start_date.strftime("%d/%m/%Y") %></strong> à partir de <%= @order.pickup_start_date.strftime("%H:%M") %> pendant un créneau de 30 minutes.
  <p>Vous récupérez votre linge propre et sec <strong><%= @order.delivery_start_date.strftime("%d/%m/%Y") %></strong> à partir de <%= @order.delivery_start_date.strftime("%H:%M") %> pendant un créneau de 30 minutes.</p>
</p>
</div>


